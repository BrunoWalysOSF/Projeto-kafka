server:
  port: 8100
spring:
  kafka:
    producer:
      bootstrap-servers: localhost:29092
  datasource:
    url: jdbc:mysql://localhost:3306/caixa?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update